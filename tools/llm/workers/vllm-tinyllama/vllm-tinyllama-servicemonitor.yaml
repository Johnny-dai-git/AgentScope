apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vllm-tinyllama
  namespace: llm
  labels:
    app: vllm-tinyllama
    vllm.role: small-model
spec:
  selector:
    matchLabels:
      app: vllm-tinyllama
  namespaceSelector:
    matchNames:
      - llm
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      honorLabels: false
