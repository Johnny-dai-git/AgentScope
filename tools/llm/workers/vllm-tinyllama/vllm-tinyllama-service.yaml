apiVersion: v1
kind: Service
metadata:
  name: vllm-tinyllama-service
  namespace: llm
  labels:
    app: vllm-tinyllama
    vllm.role: small-model
spec:
  type: ClusterIP
  selector:
    app: vllm-tinyllama
  ports:
    - name: http
      port: 8002
      targetPort: 8002
      protocol: TCP
