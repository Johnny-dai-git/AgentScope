apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vllm-qwen-small
  namespace: llm
  labels:
    app: vllm-qwen-small
    vllm.role: small-model
spec:
  selector:
    matchLabels:
      app: vllm-qwen-small
  namespaceSelector:
    matchNames:
      - llm
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      honorLabels: false
